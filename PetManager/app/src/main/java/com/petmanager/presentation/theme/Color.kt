package com.petmanager.presentation.theme

import androidx.compose.ui.graphics.Color

// ── Brand ──
val Primary = Color(0xFF2D7DD2)
val PrimaryDark = Color(0xFF1A5FA0)
val PrimaryLight = Color(0xFF5A9FE8)
val Accent = Color(0xFFF97068)
val AccentLight = Color(0xFFFFB3AE)

// ── Surface / Background ──
val Surface = Color(0xFFFAFBFC)
val SurfaceVariant = Color(0xFFF0F2F5)
val CardBackground = Color(0xFFFFFFFF)
val BackgroundLight = Color(0xFFF8FAFC)

// ── On Surface ──
val OnSurface = Color(0xFF1A1D21)
val OnSurfaceVariant = Color(0xFF6B7280)

// ── Text ──
val TextPrimary = Color(0xFF1A1D21)
val TextSecondary = Color(0xFF6B7280)
val TextHint = Color(0xFF9CA3AF)

// ── State ──
val Success = Color(0xFF10B981)
val Warning = Color(0xFFF59E0B)
val Error = Color(0xFFEF4444)
val Info = Color(0xFF3B82F6)

// ── Kakao ──
val KakaoYellow = Color(0xFFFEE500)
val KakaoText = Color(0xFF181600)

// ── Divider ──
val Divider = Color(0xFFE5E7EB)

// ── Auth Gradient ──
val GradientStart = Color(0xFF667EEA)
val GradientEnd = Color(0xFFF093FB)

// ── Dark Mode Overrides ──
val DarkSurface = Color(0xFF121212)
val DarkSurfaceVariant = Color(0xFF1E1E1E)
val DarkCardBackground = Color(0xFF1E1E1E)
val DarkOnSurface = Color(0xFFE0E0E0)
val DarkOnSurfaceVariant = Color(0xFF9E9E9E)
val DarkTextPrimary = Color(0xFFE0E0E0)
val DarkTextSecondary = Color(0xFF9E9E9E)
val DarkDivider = Color(0xFF2C2C2C)
