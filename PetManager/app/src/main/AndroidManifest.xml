<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- INTERNET: Retrofit, Kakao SDK 등 의존성에서 자동 포함됨 (중복 방지) -->

    <application
        android:name="com.petmanager.PetManagerApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Test1"
        android:networkSecurityConfig="@xml/network_security_config"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">
        <activity
            android:name="com.petmanager.presentation.ui.myinfo.MyPostsActivity"
            android:exported="false" />
        <activity
            android:name="com.petmanager.presentation.ui.chat.ChatActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize|adjustPan"/>
        <activity
            android:name="com.petmanager.presentation.ui.home.PostActivity"
            android:exported="false" />
        <activity
            android:name="com.petmanager.presentation.ui.myinfo.NicknameEditActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />
        <activity
            android:name="com.petmanager.presentation.ui.myinfo.ProfileImageEditActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />
        <activity
            android:name="com.petmanager.presentation.ui.main.MainActivity"
            android:exported="false" />
        <activity
            android:name="com.petmanager.presentation.ui.auth.SignInActivity"
            android:exported="false" />
        <activity
            android:name="com.petmanager.presentation.ui.auth.SignUpActivity"
            android:exported="false" /> <!-- 액션바 제거 android:theme="@style/Theme.AppCompat.Light.NoActionBar" -->
        <activity
            android:name="com.petmanager.presentation.ui.auth.RegionSelectionActivity"
            android:exported="false" />
        <!-- "android.intent.action.MAIN", "android.intent.category.LAUNCHER" 를 통해서 앱실행시 초기 화면 splash화면으로 설정 -->
        <activity
            android:name="com.petmanager.presentation.ui.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.PetManager.Splash">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 카카오 로그인 -->
        <activity
            android:name="com.kakao.sdk.auth.AuthCodeHandlerActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <!-- 카카오 네이티브 앱 키 - keystore.properties에서 자동으로 로드됩니다 -->
                <!-- Redirect URI: kakao{NATIVE_APP_KEY}://oauth -->
                <data
                    android:host="oauth"
                    android:scheme="kakao${KAKAO_NATIVE_APP_KEY}" />
            </intent-filter>
        </activity>

        <!-- 카카오 네이티브 앱 키 설정 -->
        <meta-data
            android:name="com.kakao.sdk.AppKey"
            android:value="${KAKAO_NATIVE_APP_KEY}" />

    </application>

</manifest>