# AndroidX (Material / Compose 등이 androidx.* 를 쓰므로 필수)
android.useAndroidX=true
android.enableJetifier=true
android.suppressUnsupportedCompileSdk=34

# API 키·서버 URL 등 민감 설정은 keystore.properties 에서만 관리합니다.
# keystore.properties.example 을 참고하세요.
